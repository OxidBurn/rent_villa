# Database
# For local development, use either:
# 1. Vercel Postgres (recommended): Run `vercel env pull .env.local`
# 2. Local PostgreSQL: postgresql://user:password@localhost:5432/rent_villa_dev
DATABASE_URL="postgresql://user:password@localhost:5432/rent_villa_dev"

# Optional: Non-pooled connection for migrations
# If not set, DATABASE_URL will be used
DATABASE_URL_MIGRATION="postgresql://user:password@localhost:5432/rent_villa_dev"

# Sentry Error Tracking
# Get these values from https://sentry.io after creating a project
# For local development, errors are logged to console (not sent to Sentry)
SENTRY_DSN="https://YOUR_SENTRY_DSN@o000000.ingest.sentry.io/0000000"
NEXT_PUBLIC_SENTRY_DSN="https://YOUR_SENTRY_DSN@o000000.ingest.sentry.io/0000000"

# Sentry Configuration (for deployment/build)
# Get auth token from: Sentry Settings → Auth Tokens
SENTRY_AUTH_TOKEN="your_sentry_auth_token_here"
SENTRY_ORG="your-organization-name"
SENTRY_PROJECT="your-project-name"
